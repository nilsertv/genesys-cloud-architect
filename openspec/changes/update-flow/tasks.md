# Tasks: `update_flow` — Edit Existing Architect Flows

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | Slice A ~300-380 (helpers ~70, tests ~180, index.ts +90, package.json +1) · Slice B ~260-310 (update-flow.ts ~181, index.ts +10, skill docs ~90) |
| 400-line budget risk | Slice A: Medium (close to budget) · Slice B: Low-Medium |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 = Slice A (Phase 1) → PR 2 = Slice B (Phase 2), stacked on main |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Deploy-runner core: `update-helpers.ts` + 12 `node:test` cases + `updateFlow()` orchestrator + `--mode` CLI flag | PR 1 | Base: `main`. Additive only, no existing export touched. Includes empirical verification. |
| 2 | MCP surface: `update-flow.ts` tool + registration + `write-flow` skill docs | PR 2 | Base: PR 1's branch (depends on `updateFlow` signature). Includes empirical verification. |

Out of scope (explicit, not silently dropped): `flowVersion` (SDK param to select "latest"/"published"/"debug"/specific version) — deferred to a future change; SDK default (`"latest"`) applies implicitly.

## Phase 1: Deploy-Runner Core (Slice A)

- [x] 1.1 Create `src/deploy-runner/update-helpers.ts` — `FlowIdentifier` type, `resolveFlowIdentifier(opts)`, `applyUpdateAndSave(flow, mutate, publish)`. Zero top-level side effects (per design.md contract). Satisfies spec: Flow Identifier Resolution, Non-Destructive Update Workflow, Lock Handling, Orphaned Lock Prevention.
  - **Post-implementation review fix (MEDIUM)**: `applyUpdateAndSave` now normalizes non-object rejections (e.g. `throw "boom"`) into an `Error` before attaching the `unlocked` signal, so the signal is never silently lost when user/SDK code throws a bare primitive. Added a covering `node:test` case (13 total now: 6 resolveFlowIdentifier + 7 applyUpdateAndSave).
- [x] 1.2 Create `src/deploy-runner/update-helpers.test.ts` with the 12 `node:test` cases from `design.md` § `node:test` Design (resolve-by-id, resolve-by-name+type, both-given precedence, name-without-type throws, type-without-name throws, neither throws, unlock-on-mutate/checkIn/publish-failure, double-failure, happy-path checkIn/publish).
- [x] 1.3 Add `"test": "node --experimental-strip-types --test src/**/*.test.ts"` to `package.json` scripts; run `pnpm test` and confirm all 12 cases pass.
- [x] 1.4 Add `updateFlow(scripting, absoluteFlowPath, opts)` export to `src/deploy-runner/index.ts`: calls `resolveFlowIdentifier`, `checkoutAndLoadFlowBy{FlowId,FlowName}Async`, dynamic-imports the flow file, calls `mod.updateFlow(scripting, flow)`, then `applyUpdateAndSave`. Widen `emit("result", ...)` payload with `unlocked`/`errorKind`. **Deviation** (see apply-progress): `flowType` is required by the SDK for both id- and name-based checkout (design.md's `FlowIdentifier` "byId" branch omits it) — `updateFlow` now requires `opts.flowType` unconditionally and throws a clear error if absent.
  - **Post-implementation review fixes** (independent fresh-context review found 3 additional issues in this task's code; see apply-progress for full detail):
    - **HIGH — orphaned lock on broken flow file**: the dynamic `import()` of the user's flow file and the check that it exports `updateFlow` now run BEFORE `checkoutAndLoadFlowBy{FlowId,FlowName}Async` (not after). Previously a broken/missing flow file would leave the checkout lock acquired with no code path that releases it, because only `applyUpdateAndSave` guarantees `unlockAsync()`.
    - **HIGH — "no `.name` on `ArchBaseFlow`" was WRONG, corrected**: the installed SDK's `types.d.ts` (`ArchBaseFlow`, ~line 13129) does declare `name: string`. Re-verified directly in `node_modules`. `updateFlow()` now returns `{ flowId: flow.id, flowName: flow.name }` (both taken from the actually-checked-out flow object, non-optional) instead of trusting caller-supplied `opts.flowName` on the byId path.
    - **HIGH — invalid `--mode` fell back to the destructive `create` path silently**: `main()` now rejects (`exit(1)` with a clear error) any `--mode` value other than exactly `"create"` or `"update"`. Only the true default (flag omitted entirely) still resolves to `"create"`, preserving `deploy_flow` backward compatibility.
- [x] 1.5 Add `--mode create|update` CLI flag (default `create`) plus `--flow-id`/`--flow-name`/`--flow-type`/`--force-unlock`/`--publish` args to `main()`; dispatch to existing `buildFlow` path or new `updateFlow` path.
- [ ] 1.6 **BLOQUEADA — sin credenciales Genesys Cloud en este entorno.** No `GENESYS_REGION`/`GENESYS_CLIENT_ID`/`GENESYS_CLIENT_SECRET` available (no `.envrc`, nothing in shell env). Empirical verification (real org, run during `sdd-apply`): deploy a flow via `deploy_flow`, then run `updateFlow` end-to-end against it in both check-in and publish modes on a real Genesys Cloud dev org. Confirm `flowId` and version history are preserved and no `createFlow<Type>Async`/delete call fires (proposal Success Criteria #1). **To unblock**: supply real `GENESYS_REGION`/`GENESYS_CLIENT_ID`/`GENESYS_CLIENT_SECRET` for a dev org with `architect:flow:edit` (and ideally `architect:flow:unlock`) permissions, then re-run this task in a follow-up `sdd-apply` batch.

## Phase 2: MCP Tool + Docs (Slice B — depends on Phase 1)

- [x] 2.1 Create `src/mcp-server/tools/update-flow.ts`: zod schema + `.refine()` (per `design.md` § Zod Schema, **corrected** — see Deviation note below), spawn `deploy-runner --mode update`, reuse `deploy-flow.ts`'s NDJSON parse loop unmodified (duplicated inline, per design decision), map `errorKind` to the 4 distinct messages in `design.md` § Differentiated Error Handling.
  - **Deviation from design.md's Zod Schema (by design, per Slice A's discovery)**: `flowType` is a required field (`z.string().min(1)`, no `.optional()`), not optional-only-for-flowName as design.md's original snippet showed. The `.refine()` only checks `Boolean(flowId) !== Boolean(flowName)` (exactly one of the two identifiers) — `flowType`'s required-ness is enforced by the base schema shape itself, independent of which identifier is used. This matches `updateFlow()`'s real signature in `index.ts` (Slice A), which throws if `opts.flowType` is absent regardless of identifier kind.
  - **Type-level deviation (new, found this session, not previously documented)**: the shared `ToolConfig`/`ToolFactory` type in `src/mcp-server/tools/types.ts` originally constrained `inputSchema: T extends ZodRawShape` (a plain raw shape only — `Record<string, ZodTypeAny>`). A `z.object({...}).refine(...)` instance is a `ZodEffects`, which does NOT structurally satisfy `ZodRawShape` (it has methods/internals, not just field keys). Design.md's claim that "the installed `@modelcontextprotocol/sdk` types `inputSchema` as `ZodRawShapeCompat | AnySchema`" is correct for the **MCP SDK's own `registerTool()`** (verified directly in `node_modules/.pnpm/@modelcontextprotocol+sdk@1.29.0_zod@4.4.3/.../mcp.d.ts`), but this project's own `ToolConfig` wrapper type was narrower than the SDK it wraps. Widened `ToolConfig`/`ToolFactory`'s generic bound from `ZodRawShape` to `ZodRawShape | ZodTypeAny` (new `ToolInputSchema` union type) in `types.ts` so `update-flow.ts` can pass the full refined schema through `inputSchema`. Backward compatible — the 3 existing tools (`flow_dependencies`, `deploy_flow`, `test_bot_flow`) still use raw shapes and are unaffected (verified: `pnpm run build` + targeted `tsc --strict` both clean).
  - **CRITICAL bug found and fixed (post-implementation, fresh-context review pass, empirically verified)**: the `.refine()`-wrapped `inputSchema` (a `ZodEffects` instance) broke MCP `tools/list` introspection. The installed `@modelcontextprotocol/sdk`'s `normalizeObjectSchema()` (`node_modules/@modelcontextprotocol/sdk/dist/esm/server/zod-compat.js`) only reads a top-level `.shape` — present on a plain object shape or a `z.object(...)` instance, but NOT on a `ZodEffects` (the `.shape` lives one level deeper, at `_def.schema.shape`). With the `.refine()`-wrapped schema, `normalizeObjectSchema()` returned `undefined` and the `tools/list` handler fell back to `EMPTY_OBJECT_JSON_SCHEMA` — so `update_flow` was announced to MCP clients (including Claude Code) with **zero declared parameters**, making the tool effectively undiscoverable to any LLM that didn't already know its argument names out of band. Validation via `tools/call` still worked (a fallback path uses the raw `ZodEffects` directly), which is why `tsc --noEmit` and biome lint both stayed green — this is a pure SDK-introspection bug, invisible to type-checking. **Fix**: reverted `inputSchema` to a flat `ZodRawShape` (`{ flowFile, flowId, flowName, flowType, forceUnlock, publish }`, no `.refine()`); moved the "exactly one of `flowId`/`flowName`" cross-field check into the handler as a manual post-parse `if`, following the same pattern already used by `deploy-flow.ts`/`flow-dependencies.ts`, returning two DISTINCT `isError: true` messages (both-given vs. neither-given) rather than one generic message. `flowType` stays required in the flat shape (`z.string().min(1)`, no `.optional()`) since it never needed the `.refine()` in the first place. Correspondingly reverted `src/mcp-server/tools/types.ts`'s `ToolConfig`/`ToolFactory` back to `ZodRawShape`-only (removed the `ToolInputSchema = ZodRawShape | ZodTypeAny` union added earlier in this task) — the widened type is no longer needed and would otherwise leave the door open for another tool to reintroduce the same bug. **Empirically verified** (not just `tsc`/lint, which had already passed before and missed this): instantiated a real `McpServer` + `Client` over `InMemoryTransport.createLinkedPair()`, registered the corrected `update_flow` tool, called `client.listTools()`, and confirmed `inputSchema.properties` now contains all 6 keys (`flowFile`, `flowId`, `flowName`, `flowType`, `forceUnlock`, `publish`) with correct types/descriptions, and `inputSchema.required` correctly lists `["flowFile", "flowType"]`. Also called `client.callTool()` with both "neither identifier given" and "both identifiers given" argument sets and confirmed the two distinct manual-validation error messages fire correctly. `pnpm test` (13/13, unaffected), `tsc --noEmit --strict` (0 errors on touched files), `pnpm run build`, and `pnpm run lint` all re-run clean after the fix.
- [x] 2.2 Register `update_flow` in `src/mcp-server/index.ts`: import, instantiate with the same config object shape as `deployFlow` (`region`/`clientId`/`clientSecret`/`deployScriptPath: envVars.DEPLOY_SCRIPT_PATH`), `server.registerTool("update_flow", ...)` immediately after `deploy_flow`'s registration.
- [x] 2.3 Update `skills/write-flow/SKILL.md`: added a "New flow vs. updating an existing flow" branch question after the "Deploy only" section, and a new "Updating an Existing Flow" section (after "Test") documenting the `update_flow` tool contract (`flowId`/`flowName`+`flowType`, `forceUnlock`, `publish`, lock-failure guidance).
- [x] 2.4 Update `skills/write-flow/references/sdk-patterns.md`: documented `updateFlow(scripting, flow): Promise<void>` (edits-only, no `checkInAsync`/`publishAsync` call) in a new "The `updateFlow` Contract" section right before "Flow Creation Methods", plus a cross-reference from "Re-creating Existing Flows".
- [x] 2.5 Update `skills/write-flow/references/gotchas.md`: promoted the `checkoutAndLoadFlowByFlowNameAsync` footnote ("Flow naming on re-runs") into a full "Editing a flow in place instead of re-creating it" section covering the always-required `flowType`, lock/unlock guarantees, and locked-by-another-user handling.
- [ ] 2.6 **BLOQUEADA — sin credenciales Genesys Cloud en este entorno** (identical blocker to 1.6, re-confirmed independently this session: `env | grep -i GENESYS` empty, no `.envrc`, no `.env*` file in repo root). Empirical verification (real org, run during a future `sdd-apply`): trigger and capture exact SDK error text/status for (a) flow locked by another user, (b) nonexistent `flowId`/`flowName`, (c) mismatched `flowType`; confirm/adjust the `errorKind` mapping in `index.ts`/`update-flow.ts` accordingly (the current `classifyUpdateError()` regex heuristic from Slice A is still unverified). Separately confirm the `architect:flow:edit` permission scope is the one actually required (test with a role lacking it, expect a 403/permission error). **To unblock**: supply real `GENESYS_REGION`/`GENESYS_CLIENT_ID`/`GENESYS_CLIENT_SECRET` for a dev org with `architect:flow:edit` (and ideally `architect:flow:unlock`) permissions, then re-run this task in a follow-up `sdd-apply` batch (can run alongside re-running 1.6 in the same batch, since both need the same credentials against the same org).
