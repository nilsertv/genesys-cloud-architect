change: flow-ir
created: 2026-09-04
execution_mode: auto
artifact_store: openspec
delivery_strategy: auto-chain
chain_strategy: stacked-to-main
review_budget_lines: 400

phases:
  explore:
    status: done
    artifact: openspec/changes/flow-ir/exploration.md
  proposal:
    status: done
    artifact: openspec/changes/flow-ir/proposal.md
  design:
    status: done
    artifact: openspec/changes/flow-ir/design.md
  specs:
    status: done
    artifact: openspec/changes/flow-ir/specs/flow-ir/spec.md
  tasks:
    status: done
    artifact: openspec/changes/flow-ir/tasks.md
  apply:
    status: in_progress
    artifact: openspec/changes/flow-ir/apply-progress.md
    note: >
      PR1 partially done: openspec/config.yaml testing-section fix done
      (uncommitted). Real fixture capture (tasks 1.1-1.3, 1.5) blocked —
      no Genesys credentials available in this environment. All commits
      on this branch (port commit + PR1 commit) blocked by husky
      pre-commit's `pnpm build` failing 401 on the private
      @makingchatbots devDependency (expected until PR3 removes it). PR2
      (flow-ir-parser.ts) and PR3 (raw-action-lookup.ts + wiring) not
      started.
  verify:
    status: pending
  archive:
    status: pending
